## Стек технологий

- **Frontend**: React 18, Vite, TypeScript, Ant Design, TanStack Query (React Query), Axios.
- **Архитектура**: Feature-Sliced Design (FSD).
- **Backend**: Laravel 11, PHP 8.2, MySQL.
- **Infrastructure**: Docker, Docker Compose, Nginx.

## Установка и запуск

### 1. Подготовка окружения

Убедитесь, что у вас установлен Docker и Docker Compose.

### 2. Запуск контейнеров

Склонируйте репозиторий и запустите сборку из корневой папки:

```bash
docker compose up -d --build
```

Настройка Backend
После того как контейнеры поднимутся, необходимо накатить миграции и наполнить базу тестовыми данными:

Bash

# Генерация ключа приложения

docker compose exec backend php artisan key:generate

# Запуск миграций и сидеров

docker compose exec backend php artisan migrate:fresh --seed

📍 Доступы
Frontend: http://localhost:5173

Backend API: http://localhost:8080/api

✅ Реализованный функционал
Просмотр ленты статей.

Переход на детальную страницу статьи.

Чтение и добавление комментариев.

Создание новых публикаций через форму.
**«Затраченное время 4 часа»**
